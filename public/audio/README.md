Drop a file named `dark-knight-rises.mp3` (the Dark Knight Rises theme, ~the last ~60s looped) into this folder. It will be played looped by the sound toggle.

The site will keep using a synthesized dark-melody fallback if the file is missing, but for the real theme you must add it here (copyright prevents shipping it in the repo).

Name it exactly: `public/audio/dark-knight-rises.mp3`
