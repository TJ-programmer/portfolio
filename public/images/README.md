Drop a photo of Batman here named exactly `batman.jpg`.

It becomes the cinematic "2.5D" centerpiece in the 3D scene — mouse parallax, breathing scale, bat-signal rim glow, film grain shader, vignette, and a gold halo behind it. The procedural 3D figure is used only if this photo is missing.

Suggestions:
- A single, full-body (or waist-up) portrait with a dark background works best.
- Name it exactly: `public/images/batman.jpg`
- Anything over ~3000px wide will be scaled down; ~1200-2000px is ideal.
